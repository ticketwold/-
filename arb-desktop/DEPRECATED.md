# DEPRECATED — use chrome-extension v2.0.0+

**ArbDesktop.exe / PyQt / WebSocket Bridge는 더 이상 사용하지 않습니다.**

Chrome 확장프로그램 단독 버전으로 이전했습니다:

- 폴더: `/chrome-extension/`
- 배포: `arb-extension-v2.0.0.zip`
- 설치: `chrome://extensions` → 개발자 모드 → 압축해제된 확장 프로그램 로드

이 디렉터리(`arb-desktop/`)는 레거시 참조용으로만 유지됩니다.
